This code ontains AI-concepts like:
Relu maximum function that's like a filter to only let positive numbers pass
normalization is necessary to avoid one column dominating the model. Normalization scales them like -1 to 1.
Gradient 
MSE loss function is mean of (y_pred-y_true)^2
